---
import HTML from "~/components/HTML.astro";

type Props = {
  title?: string;
  description?: string;
};

const { title, description } = Astro.props;
---

<HTML {title} {description}>
  <Fragment slot="body">
    <slot />
  </Fragment>
</HTML>
